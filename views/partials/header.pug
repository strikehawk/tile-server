.navbar.navbar-default.navbar-fixed-top
  .container
    .navbar-header
      button.navbar-toggle(type='button', data-toggle='collapse', data-target='.navbar-collapse')
        span.sr-only Toggle navigation
        span.icon-bar
        span.icon-bar
        span.icon-bar
      a.navbar-brand(href='/')
        i.fa.fa-rebel
        | Tile Server
    .collapse.navbar-collapse
      ul.nav.navbar-nav
        li(class=(`${screen}` == 'home') ? 'active' : undefined)
          a(href='/') Home
        li(class=(`${screen}` == 'sources') ? 'active' : undefined)
          a(href='/sources') Sources
        li(class=(`${screen}` == 'create-source') ? 'active' : undefined)
          a(href='/sources/create') New source